[project]
name = "AssetManager"
version = "0.1.0"

[target.AssetManager]
type = "staticlib"
sources = ["src/**/*.cpp"]
include_dirs = ["include"]

[[target.AssetManager.deps]]
system = "libsodium"

[[target.AssetManager.deps]]
system = "libzstd"
